spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/bloodbank_backend}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:nithin}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8085}

frontend.url=${FRONTEND_URL:http://localhost:5173}

# Mail Configuration
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:nithinreddythadi999@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:xmtnmrdiakugzgwl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${SPRING_MAIL_HOST:smtp.gmail.com}

# Gemini integration settings
gemini.api.key=${GEMINI_API_KEY:AIzaSyBqVGa-P95Y8hMkmY56hJu1atcYYq47Mxg}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/}
gemini.model=${GEMINI_MODEL:gemini-2.5-flash}

# JWT settings
jwt.secret=${JWT_SECRET:my-very-secret-key-which-should-be-very-long}
jwt.expirationMs=${JWT_EXPIRATION_MS:3600000}
